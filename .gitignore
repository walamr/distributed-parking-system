# ── Build outputs ───────────────────────────────────────────────────────────
.gradle/
build/
*/build/
**/build/
bin/
**/bin/
*.class
dist/

# ── IDE / OS ─────────────────────────────────────────────────────────────────
.project
.classpath
.settings/
.vscode/
.idea/
*.iml
*.ipr
*.iws
.DS_Store
Thumbs.db
project.pdf
project_text.txt


# ── Logs / generated ─────────────────────────────────────────────────────────
*.log
logs/
*.zip
red-team-delivery/
gradle-bin/

# ── SECRETS — NEVER COMMIT ───────────────────────────────────────────────────
# Environment files with real credentials
.env
network-ips.env
env-configs/*.env

# TLS private keys & keystores (self-signed lab certs — regenerate for each env)
docker/rabbitmq/certs/*-key.pem
docker/rabbitmq/certs/*.jks
docker/rabbitmq/certs/*.p12
docker/rabbitmq/certs/*.srl

# MongoDB private certs and keyfile
docker/mongodb/certs/mongo*.pem
docker/mongodb/certs/mongodb-keyfile