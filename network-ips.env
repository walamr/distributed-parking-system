# =============================================================
#  Network Configuration Template  —  fill in your own IPs
#  DO NOT commit this file with real values (it is gitignored)
# =============================================================

# ── MongoDB IP addresses ──────────────────────────────────────
MONGO1_IP=<YOUR_MONGO1_IP>
MONGO2_IP=<YOUR_MONGO2_IP>
MONGO3_IP=<YOUR_MONGO3_IP>

# ── RabbitMQ IP addresses ─────────────────────────────────────
RABBIT1_IP=<YOUR_RABBIT1_IP>
RABBIT2_IP=<YOUR_RABBIT2_IP>
RABBIT3_IP=<YOUR_RABBIT3_IP>

# ── UI IP addresses ───────────────────────────────────────────
CUSTOMER_IP=<YOUR_CUSTOMER_IP>
PEO_IP=<YOUR_PEO_IP>
MO_IP=<YOUR_MO_IP>

# ── Recommender IP addresses ──────────────────────────────────
RECOMMENDER1_IP=<YOUR_RECOMMENDER1_IP>
RECOMMENDER2_IP=<YOUR_RECOMMENDER2_IP>
RECOMMENDER3_IP=<YOUR_RECOMMENDER3_IP>

# ── Shared application secret ─────────────────────────────────
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
HMAC_SECRET=REPLACE_WITH_SHARED_HMAC_SECRET_MIN_32_CHARS