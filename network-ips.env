# =============================================================
#  Main Network Configuration - Fill in your IPs here
# =============================================================

# ── Database (MongoDB) IP addresses ──────────────
MONGO1_IP=127.0.0.1
MONGO2_IP=127.0.0.1
MONGO3_IP=127.0.0.1

# ── Message Queue (RabbitMQ) IP addresses ──────────────
RABBIT1_IP=127.0.0.1
RABBIT2_IP=127.0.0.1
RABBIT3_IP=127.0.0.1

# ── UI IP addresses ────────────────────────
CUSTOMER_IP=127.0.0.1
PEO_IP=127.0.0.1
MO_IP=127.0.0.1

# ── Recommender IP addresses ───────────────
RECOMMENDER1_IP=127.0.0.1
RECOMMENDER2_IP=127.0.0.1
RECOMMENDER3_IP=127.0.0.1

