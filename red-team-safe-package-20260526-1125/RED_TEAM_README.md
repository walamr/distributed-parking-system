# Red Team Runtime Package

This package excludes repository history and Java source code. It includes built JARs/distributions, Docker Compose, RabbitMQ/MongoDB configs, setup scripts, seed data, local demo certificates, and security reports.

Use the docs in `docs/` first. The included passwords/certificates are disposable local demo credentials only. MongoDB requires local port `127.0.0.1:27017` to be free.
